# Claude Code 能力说明

## 模型信息

默认支持以下 Claude 模型系列（当前环境使用 deepseek-v4-pro）：

| 模型 | 特点 |
|------|------|
| Claude Opus 4.7 | 最强推理能力，适合复杂架构设计 |
| Claude Sonnet 4.6 | 平衡性能与速度，适合日常开发 |
| Claude Haiku 4.5 | 轻量快速，适合简单任务 |

## 代码操作

- **文件读写**：读取、创建、编辑任意代码文件
- **精确编辑**：基于 `Edit` 工具的字符串级替换，支持全局替换
- **代码搜索**：`grep` 搜索符号/关键字，`find` 按文件名模式查找
- **多文件重构**：跨文件重命名、接口修改、依赖更新

## 项目管理

- **Git 操作**：`git status`、`git diff`、`git log`、提交、分支管理
- **PR 创建**：通过 `gh` CLI 创建 Pull Request，自动生成标题和描述
- **代码审查**：Review PR 变更，提供安全审查

## 构建与测试

- **Shell 执行**：运行 `cmake`、`make`、`colcon build` 等构建命令
- **测试运行**：执行单元测试、集成测试
- **依赖管理**：安装/更新系统包和项目依赖

## 探索与规划

- **子 Agent**：启动多个并行 Agent 分头探索代码库
- **规划模式**：复杂任务先设计方案，经确认后执行
- **Todo 跟踪**：多步骤任务自动创建和跟踪进度

## 其他能力

- **Web 搜索**：获取最新技术文档和资料
- **Jupyter Notebook**：读取和编辑 `.ipynb` 文件
- **PDF 阅读**：读取 PDF 论文和文档
- **图片查看**：查看截图进行 UI 分析
- **定时任务**：设置周期性检查或提醒
- **持久记忆**：跨会话记住项目上下文和用户偏好

---

## 使用提示

- **直接提出需求**：我会自动识别并执行，无需手动指定工具
- **复杂任务先规划**：我会先用规划模式设计方案，等你确认再动手
- **并行处理**：独立任务会自动并行执行以提升效率
- **安全第一**：破坏性操作（如 `git push --force`、`rm -rf`）会先征求确认
